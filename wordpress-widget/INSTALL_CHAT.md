# 📦 Установка встроенного чата Telegram-бота на WordPress

## 🎯 Что создано

1. **API endpoint** (`/api/chat.php`) - принимает сообщения от веб-чата
2. **WebChatHandler** (`bot/WebChatHandler.php`) - обрабатывает сообщения без Telegram API
3. **WordPress плагин** (`bestweb-telegram-bot-chat.php`) - встроенный чат-виджет
4. **Стили и скрипты** - полноценный UI чата

## 📋 Шаги установки

### 1. Установка API на сервере бота

```bash
# Перейдите в директорию проекта бота
cd /ssd/www/bots/botsalebestwebstudio

# Проверьте, что файлы созданы
ls -la api/chat.php
ls -la bot/WebChatHandler.php

# Установите права
chmod 644 api/chat.php
chmod 644 bot/WebChatHandler.php
```

### 2. Настройка Apache для API

Убедитесь, что в конфигурации Apache разрешен доступ к `/api/`:

```apache
<Directory /ssd/www/bots/botsalebestwebstudio/api>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
```

Перезапустите Apache:
```bash
sudo systemctl restart apache2
```

### 3. Установка WordPress плагина

```bash
# Путь к WordPress (уточните у администратора)
WORDPRESS_PATH="/ssd/www/best-web-studio.ru"

# Копируем плагин
cp -r wordpress-widget "$WORDPRESS_PATH/wp-content/plugins/bestweb-telegram-bot-chat"

# Устанавливаем права
chown -R www-data:www-data "$WORDPRESS_PATH/wp-content/plugins/bestweb-telegram-bot-chat"
chmod -R 755 "$WORDPRESS_PATH/wp-content/plugins/bestweb-telegram-bot-chat"
```

### 4. Активация плагина

1. Зайдите в админку WordPress: `https://best-web-studio.ru/wp-admin`
2. Перейдите в **Плагины** (Plugins)
3. Найдите **"BestWeb Telegram Bot Chat Widget"**
4. Нажмите **"Активировать"**

### 5. Настройка плагина

1. Перейдите в **Настройки → Telegram Bot Chat**
2. Заполните форму:
   - **API URL:** `https://botsale.1tlt.ru/api/chat.php`
   - **Enable Chat Widget:** ✓ Включено
   - **Widget Position:** Bottom Right
   - **Chat Title (Russian):** Чат с менеджером
   - **Chat Title (English):** Chat with manager
   - **Chat Title (Thai):** แชทกับผู้จัดการ
3. Нажмите **"Сохранить изменения"**

## ✅ Проверка работы

1. Откройте сайт https://best-web-studio.ru/
2. В правом нижнем углу должна появиться синяя круглая кнопка с иконкой Telegram
3. При клике должно открыться окно чата
4. Напишите сообщение и проверьте, что бот отвечает

## 🔧 Технические детали

### API Endpoint

- **URL:** `https://botsale.1tlt.ru/api/chat.php`
- **Метод:** POST
- **Формат:** JSON
- **Параметры:**
  ```json
  {
    "session_id": "web_xxx",
    "message": "Привет",
    "lang": "ru",
    "name": "Website User"
  }
  ```

### Ответ API

```json
{
  "success": true,
  "session_id": "web_xxx",
  "response": "Привет! 👋\n\nЯ помогу вам...",
  "keyboard": null,
  "step": "greeting"
}
```

## 🐛 Решение проблем

### Чат не открывается

1. Проверьте консоль браузера (F12 → Console) на наличие ошибок
2. Убедитесь, что плагин активирован
3. Проверьте, что API URL правильный

### Бот не отвечает

1. Проверьте логи Apache:
   ```bash
   tail -f /var/log/apache2/botsalebestwebstudio_error.log
   ```
2. Проверьте доступность API:
   ```bash
   curl -X POST https://botsale.1tlt.ru/api/chat.php \
     -H "Content-Type: application/json" \
     -d '{"session_id":"test","message":"test","lang":"ru"}'
   ```
3. Убедитесь, что база данных доступна

### CORS ошибки

Если возникают CORS ошибки, добавьте в `api/chat.php`:
```php
header('Access-Control-Allow-Origin: https://best-web-studio.ru');
```

## 📝 Важные замечания

1. **Session ID:** Сохраняется в localStorage браузера, уникален для каждого пользователя
2. **Виртуальные пользователи:** Создаются в БД с `telegram_id = -1000000000`
3. **Web Chat ID:** Используются отрицательные chat_id для веб-чата
4. **Мультиязычность:** Автоматически определяется язык сайта

## 🎉 Готово!

После установки и настройки встроенный чат будет работать на всех страницах сайта!
