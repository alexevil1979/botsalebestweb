# ✅ Руководство по тестированию бота

## 🎉 Бот работает!

Бот успешно отвечает на команду `/start` и начинает диалог.

## 📋 Проверка сохранения данных

### 1. Проверка в админ-панели

1. **Откройте админ-панель:** `https://botsale.1tlt.ru/admin/`
2. **Войдите** с паролем: `qweasd333123`
3. **Проверьте разделы:**
   - **Диалоги** - должен появиться новый диалог со статусом "active"
   - **Пользователи** - должен появиться новый пользователь
   - **Сообщения** - должны сохраняться все сообщения (входящие и исходящие)

### 2. Проверка базы данных

```bash
# Проверка последних пользователей
mysql -u telegram_bot -p telegram_bot -e "
SELECT u.id, u.telegram_id, u.first_name, u.username, u.language_code, u.created_at 
FROM users u 
ORDER BY u.created_at DESC 
LIMIT 5;
"

# Проверка последних диалогов
mysql -u telegram_bot -p telegram_bot -e "
SELECT d.id, d.user_id, d.current_step, d.status, d.created_at 
FROM dialogs d 
ORDER BY d.created_at DESC 
LIMIT 5;
"

# Проверка последних сообщений
mysql -u telegram_bot -p telegram_bot -e "
SELECT m.id, m.dialog_id, m.direction, LEFT(m.text, 50) as text, m.created_at 
FROM messages m 
ORDER BY m.created_at DESC 
LIMIT 10;
"
```

### 3. Проверка Redis

```bash
# Проверка данных в Redis
redis-cli

# В Redis CLI выполните:
KEYS dialog:*
# Должны быть ключи для активных диалогов

# Просмотр данных диалога (замените USER_ID на реальный ID)
GET dialog:USER_ID:step
GET dialog:USER_ID:data

# Выход из Redis CLI
exit
```

## 🧪 Тестирование воронки продаж

### Шаг 1: Приветствие
- ✅ Бот отвечает на `/start`
- ✅ Сохраняется пользователь в БД
- ✅ Создается диалог в БД

### Шаг 2: Описание задачи
1. **Отправьте боту** описание задачи, например: "Мне нужен интернет-магазин"
2. **Проверьте:**
   - Бот должен ответить и уточнить детали
   - Сообщение должно сохраниться в БД
   - Шаг диалога должен обновиться в Redis

### Шаг 3: Уточнение деталей
1. **Ответьте на вопросы бота**
2. **Проверьте:**
   - Бот задает уточняющие вопросы
   - Данные сохраняются в Redis

### Шаг 4: Выбор услуги
1. **Бот должен предложить услуги**
2. **Выберите услугу**
3. **Проверьте:**
   - Бот показывает услуги из БД
   - Выбранная услуга сохраняется

### Шаг 5: Бюджет
1. **Бот спросит о бюджете**
2. **Укажите бюджет**
3. **Проверьте:**
   - Бюджет сохраняется

### Шаг 6: Контакты
1. **Бот попросит контакты**
2. **Укажите телефон и/или email**
3. **Проверьте:**
   - Создается лид в БД
   - Лид отображается в админ-панели

### Шаг 7: Завершение
1. **Бот должен поблагодарить**
2. **Проверьте:**
   - Диалог завершается
   - Лид создан со статусом "new"

## 📊 Проверка лидов

```bash
# Проверка созданных лидов
mysql -u telegram_bot -p telegram_bot -e "
SELECT l.id, l.user_id, l.service_id, l.budget_from, l.budget_to, l.phone, l.email, l.status, l.created_at 
FROM leads l 
ORDER BY l.created_at DESC 
LIMIT 5;
"
```

## 🔍 Проверка логов

```bash
# Просмотр логов в реальном времени
sudo tail -f /var/log/apache2/botsalebestwebstudio_error.log

# Просмотр последних ошибок
sudo tail -n 50 /var/log/apache2/botsalebestwebstudio_error.log
```

## ✅ Чек-лист тестирования

- [x] Бот отвечает на `/start`
- [ ] Пользователь сохраняется в БД
- [ ] Диалог создается в БД
- [ ] Сообщения сохраняются в БД
- [ ] Бот задает вопросы по воронке
- [ ] Услуги отображаются из БД
- [ ] Бюджет сохраняется
- [ ] Контакты сохраняются
- [ ] Лид создается в БД
- [ ] Лид отображается в админ-панели

## 🔧 Если что-то не работает

### Бот не отвечает на сообщения

```bash
# Проверка логов
sudo tail -f /var/log/apache2/botsalebestwebstudio_error.log

# Проверка подключения к БД
php -r "require 'vendor/autoload.php'; Core\Config::load('.env'); Core\Database::fetch('SELECT 1');"

# Проверка подключения к Redis
redis-cli ping
```

### Сообщения не сохраняются

```bash
# Проверка прав доступа
ls -la /ssd/www/bots/botsalebestwebstudio/

# Проверка подключения к БД
mysql -u telegram_bot -p telegram_bot -e "SELECT 1;"
```

### Лиды не создаются

```bash
# Проверка таблицы leads
mysql -u telegram_bot -p telegram_bot -e "DESCRIBE leads;"

# Проверка услуг
mysql -u telegram_bot -p telegram_bot -e "SELECT * FROM services;"
```

---

**✅ Продолжайте тестировать воронку продаж!**
