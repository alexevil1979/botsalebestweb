# 🚀 Дорожная карта доработок Telegram-бота для продаж веб-студии

## 📊 Приоритеты

- 🔴 **Высокий** - критически важно для работы
- 🟡 **Средний** - улучшит эффективность
- 🟢 **Низкий** - nice to have

---

## 1. 🔴 Уведомления менеджерам

### Описание
Автоматическая отправка уведомлений менеджерам при создании нового лида.

### Функционал
- Уведомление в Telegram менеджеру (@Branch) при создании лида
- Email-уведомления (опционально)
- Настройка получателей в админ-панели
- Шаблоны уведомлений

### Технические детали
- Добавить таблицу `managers` (telegram_id, email, active, notification_types)
- Создать класс `Core\Notification`
- Интеграция с Telegram API для отправки сообщений менеджерам
- Настройки в админ-панели (`admin/settings.php`)

### Файлы для создания/изменения
- `core/Notification.php` (новый)
- `admin/settings.php` (новый)
- `migrations/add_managers_table.php` (новый)
- `bot/WebhookHandler.php` (изменить - добавить уведомление после создания лида)

---

## 2. 🔴 Экспорт данных

### Описание
Возможность экспортировать лиды, диалоги, статистику в различные форматы.

### Функционал
- Экспорт лидов в CSV/Excel
- Экспорт диалогов в CSV
- Экспорт статистики в PDF
- Фильтры для экспорта (дата, статус, услуга)
- Массовый экспорт

### Технические детали
- Использовать библиотеку `PhpSpreadsheet` для Excel
- Использовать `TCPDF` или `FPDF` для PDF
- Добавить кнопки экспорта в админ-панели
- Endpoint `/admin/export.php?type=leads&format=csv`

### Файлы для создания/изменения
- `admin/export.php` (новый)
- `core/Exporter.php` (новый)
- `composer.json` (добавить зависимости)
- `admin/leads.php` (добавить кнопку экспорта)
- `admin/dialogs.php` (добавить кнопку экспорта)

---

## 3. 🟡 Расширенная аналитика

### Описание
Улучшенная аналитика с графиками, трендами и метриками.

### Функционал
- Графики конверсии по этапам воронки
- Статистика по услугам (популярность, конверсия)
- Воронка продаж визуализация
- Метрики по времени (среднее время на этапе, время до конверсии)
- Сравнение периодов (день/неделя/месяц)
- Топ услуг по конверсии
- Статистика по языкам пользователей

### Технические детали
- Использовать `Chart.js` для графиков
- Создать API endpoints для данных (`admin/api/analytics.php`)
- Кэширование агрегированных данных в Redis
- Таблица `analytics_cache` для хранения предрасчитанных метрик

### Файлы для создания/изменения
- `admin/analytics.php` (новый)
- `admin/api/analytics.php` (новый)
- `core/Analytics.php` (новый)
- `admin/assets/charts.js` (новый)
- `admin/index.php` (расширить дашборд)

---

## 4. 🟡 Обработка возражений

### Описание
Улучшенная обработка возражений пользователей в боте.

### Функционал
- Распознавание типичных возражений ("дорого", "не нужно", "подумаю")
- Автоматические ответы на возражения
- Сохранение возражений в БД для анализа
- Шаблоны ответов на возражения (редактируемые в админке)
- Использование LLM для генерации ответов на возражения

### Технические детали
- Добавить таблицу `objections` (text, category, response_template)
- Класс `Core\ObjectionHandler`
- Интеграция с LLM для улучшения ответов
- Настройка шаблонов в админ-панели

### Файлы для создания/изменения
- `core/ObjectionHandler.php` (новый)
- `migrations/add_objections_table.php` (новый)
- `admin/objections.php` (новый)
- `bot/WebhookHandler.php` (добавить обработку возражений)

---

## 5. 🟡 Автоматические напоминания

### Описание
Автоматическая отправка напоминаний пользователям, которые не завершили воронку.

### Функционал
- Напоминание через 24 часа, если пользователь не ответил
- Напоминание через 3 дня, если лид не обработан
- Настройка интервалов напоминаний
- Шаблоны напоминаний
- Отключение напоминаний для пользователей

### Технические детали
- Cron job для проверки неактивных диалогов
- Таблица `reminders` (dialog_id, type, scheduled_at, sent_at)
- Класс `Core\Reminder`
- Скрипт `cron/check-reminders.php`

### Файлы для создания/изменения
- `core/Reminder.php` (новый)
- `cron/check-reminders.php` (новый)
- `migrations/add_reminders_table.php` (новый)
- `admin/settings.php` (настройки напоминаний)

---

## 6. 🟡 Интеграция с CRM

### Описание
Интеграция с популярными CRM системами (amoCRM, Bitrix24, HubSpot).

### Функционал
- Автоматическая отправка лидов в CRM
- Синхронизация статусов
- Маппинг полей
- Webhook для обновлений из CRM

### Технические детали
- Класс `Core\CRM\BaseCRM` (абстрактный)
- Реализации: `Core\CRM\AmoCRM`, `Core\CRM\Bitrix24`, `Core\CRM\HubSpot`
- Настройки в админ-панели
- Таблица `crm_sync` для отслеживания синхронизации

### Файлы для создания/изменения
- `core/CRM/BaseCRM.php` (новый)
- `core/CRM/AmoCRM.php` (новый)
- `admin/crm-settings.php` (новый)
- `migrations/add_crm_sync_table.php` (новый)

---

## 7. 🟢 A/B тестирование

### Описание
Тестирование различных вариантов текстов и воронок.

### Функционал
- Создание вариантов текстов (A/B)
- Распределение пользователей по вариантам
- Отслеживание конверсии по вариантам
- Статистика эффективности

### Технические детали
- Таблица `ab_tests` (name, variants, distribution, active)
- Таблица `ab_test_results` (test_id, user_id, variant, converted)
- Класс `Core\ABTest`
- Настройка в админ-панели

### Файлы для создания/изменения
- `core/ABTest.php` (новый)
- `admin/ab-tests.php` (новый)
- `migrations/add_ab_tests_tables.php` (новый)
- `bot/WebhookHandler.php` (интеграция A/B тестов)

---

## 8. 🟢 Обработка медиа-файлов

### Описание
Обработка изображений, документов, голосовых сообщений от пользователей.

### Функционал
- Сохранение файлов от пользователей
- Просмотр файлов в админ-панели
- Отправка примеров работ (портфолио) через бота
- Обработка голосовых сообщений (транскрипция)

### Технические детали
- Таблица `attachments` (message_id, file_id, file_type, file_path)
- Хранение файлов в `uploads/`
- Интеграция с Telegram API для скачивания файлов
- Опционально: интеграция с Whisper API для транскрипции

### Файлы для создания/изменения
- `core/Attachment.php` (новый)
- `admin/attachments.php` (новый)
- `migrations/add_attachments_table.php` (новый)
- `bot/WebhookHandler.php` (обработка медиа)

---

## 9. 🟢 Мультименеджер

### Описание
Распределение лидов между несколькими менеджерами.

### Функционал
- Назначение лидов менеджерам
- Автоматическое распределение (round-robin, по загрузке)
- История работы с лидом
- Комментарии менеджеров

### Технические детали
- Добавить поле `assigned_manager_id` в таблицу `leads`
- Таблица `lead_history` для отслеживания изменений
- Класс `Core\LeadAssignment`
- Интерфейс назначения в админ-панели

### Файлы для создания/изменения
- `core/LeadAssignment.php` (новый)
- `migrations/add_lead_assignment.php` (новый)
- `admin/leads.php` (добавить назначение менеджера)
- `admin/lead.php` (показать назначенного менеджера)

---

## 10. 🟢 Резервное копирование

### Описание
Автоматическое резервное копирование базы данных и файлов.

### Функционал
- Ежедневное резервное копирование БД
- Резервное копирование файлов
- Хранение на удаленном сервере (S3, FTP)
- Восстановление из резервной копии
- Настройка расписания

### Технические детали
- Скрипт `cron/backup.php`
- Использование `mysqldump` для БД
- Интеграция с AWS S3 или FTP
- Настройки в админ-панели

### Файлы для создания/изменения
- `cron/backup.php` (новый)
- `core/Backup.php` (новый)
- `admin/backup.php` (новый)
- `admin/settings.php` (настройки бэкапа)

---

## 11. 🟢 API для внешних систем

### Описание
REST API для интеграции с внешними системами.

### Функционал
- Получение списка лидов
- Создание лида через API
- Обновление статуса лида
- Получение статистики
- Аутентификация через API ключи

### Технические детали
- Таблица `api_keys` (key, name, permissions, active)
- Класс `Core\API\Router`
- Endpoints: `/api/v1/leads`, `/api/v1/stats`, `/api/v1/dialogs`
- Документация API (Swagger/OpenAPI)

### Файлы для создания/изменения
- `api/v1/router.php` (новый)
- `core/API/Auth.php` (новый)
- `core/API/LeadsController.php` (новый)
- `migrations/add_api_keys_table.php` (новый)
- `admin/api-keys.php` (новый)

---

## 12. 🟢 Платежи и счета

### Описание
Интеграция платежных систем для приема оплаты через бота.

### Функционал
- Генерация счетов
- Интеграция с платежными системами (ЮKassa, Stripe)
- Отслеживание оплат
- История транзакций

### Технические детали
- Таблица `invoices` (lead_id, amount, status, payment_id)
- Таблица `payments` (invoice_id, amount, method, status, created_at)
- Класс `Core\Payment`
- Интеграция с платежными API

### Файлы для создания/изменения
- `core/Payment.php` (новый)
- `migrations/add_payments_tables.php` (новый)
- `admin/payments.php` (новый)
- `bot/WebhookHandler.php` (обработка платежей)

---

## 13. 🟢 Календарь и записи

### Описание
Система записи на консультации и встречи через бота.

### Функционал
- Выбор времени для консультации
- Календарь доступности менеджеров
- Напоминания о встречах
- Интеграция с Google Calendar

### Технические детали
- Таблица `appointments` (lead_id, manager_id, scheduled_at, status)
- Класс `Core\Appointment`
- Интеграция с Google Calendar API

### Файлы для создания/изменения
- `core/Appointment.php` (новый)
- `migrations/add_appointments_table.php` (новый)
- `admin/appointments.php` (новый)
- `bot/WebhookHandler.php` (выбор времени)

---

## 14. 🟢 Улучшенное логирование

### Описание
Расширенная система логирования для отладки и мониторинга.

### Функционал
- Структурированные логи (JSON)
- Уровни логирования (DEBUG, INFO, WARNING, ERROR)
- Ротация логов
- Поиск по логам в админ-панели
- Алерты при критических ошибках

### Технические детали
- Использование `Monolog` для логирования
- Таблица `logs` для хранения важных событий
- Класс `Core\Logger`
- Интерфейс просмотра логов в админке

### Файлы для создания/изменения
- `core/Logger.php` (новый)
- `admin/logs.php` (новый)
- `migrations/add_logs_table.php` (новый)
- `composer.json` (добавить monolog/monolog)

---

## 15. 🟢 Многоязычная админ-панель

### Описание
Поддержка нескольких языков в админ-панели.

### Функционал
- Переключение языка интерфейса
- Переводы всех текстов
- Сохранение выбранного языка

### Технические детали
- Расширить `Core\Translator` для админки
- Файлы переводов `admin/translations/`
- Сохранение языка в сессии

### Файлы для создания/изменения
- `admin/translations/ru.php` (новый)
- `admin/translations/en.php` (новый)
- `admin/includes/header.php` (переключатель языка)
- `core/Translator.php` (расширить)

---

## 16. 🟢 Статистика по услугам

### Описание
Детальная аналитика по каждой услуге.

### Функционал
- Популярность услуг (сколько раз выбирали)
- Конверсия по услугам (сколько лидов создано)
- Средний бюджет по услуге
- Время до конверсии по услуге

### Технические детали
- Расширить `admin/analytics.php`
- Добавить фильтры по услугам
- Графики по услугам

### Файлы для создания/изменения
- `admin/analytics.php` (расширить)
- `core/Analytics.php` (добавить методы по услугам)

---

## 17. 🟢 Временные зоны

### Описание
Учет временных зон пользователей для корректного отображения времени.

### Функционал
- Определение временной зоны пользователя
- Отображение времени в его зоне
- Настройка рабочего времени с учетом зон

### Технические детали
- Добавить поле `timezone` в таблицу `users`
- Класс `Core\Timezone`
- Использование `DateTimeZone` PHP

### Файлы для создания/изменения
- `core/Timezone.php` (новый)
- `migrations/add_timezone_to_users.php` (новый)
- `admin/leads.php` (отображение времени с учетом зоны)

---

## 18. 🟢 Расписание работы

### Описание
Настройка рабочего времени и автоматические ответы вне рабочего времени.

### Функционал
- Настройка рабочих часов
- Автоматическое сообщение вне рабочего времени
- Учет выходных дней
- Разные расписания для разных языков/регионов

### Технические детали
- Таблица `working_hours` (day_of_week, start_time, end_time, timezone)
- Класс `Core\WorkingHours`
- Проверка времени перед отправкой сообщений

### Файлы для создания/изменения
- `core/WorkingHours.php` (новый)
- `migrations/add_working_hours_table.php` (новый)
- `admin/settings.php` (настройка расписания)
- `bot/WebhookHandler.php` (проверка рабочего времени)

---

## 19. 🟢 Автоответы

### Описание
Автоматические ответы на типичные вопросы пользователей.

### Описание
- База знаний с ответами
- Распознавание вопросов (ключевые слова, LLM)
- Автоматический ответ
- Обучение на основе диалогов

### Технические детали
- Таблица `faq` (question, answer, keywords, language)
- Класс `Core\AutoReply`
- Интеграция с LLM для распознавания вопросов

### Файлы для создания/изменения
- `core/AutoReply.php` (новый)
- `migrations/add_faq_table.php` (новый)
- `admin/faq.php` (новый)
- `bot/WebhookHandler.php` (проверка автоответов)

---

## 20. 🟢 Интеграция с мессенджерами

### Описание
Расширение на другие мессенджеры (WhatsApp, Viber).

### Функционал
- Поддержка WhatsApp Business API
- Поддержка Viber
- Единая логика для всех мессенджеров
- Переключение между каналами

### Технические детали
- Абстрактный класс `Core\Messenger\BaseMessenger`
- Реализации: `Core\Messenger\Telegram`, `Core\Messenger\WhatsApp`, `Core\Messenger\Viber`
- Таблица `channels` для хранения информации о каналах

### Файлы для создания/изменения
- `core/Messenger/BaseMessenger.php` (новый)
- `core/Messenger/WhatsApp.php` (новый)
- `core/Messenger/Viber.php` (новый)
- `migrations/add_channels_table.php` (новый)

---

## 📋 Приоритизация по бизнес-ценности

### Фаза 1 (Критично - первые 2-4 недели)
1. Уведомления менеджерам
2. Экспорт данных
3. Расширенная аналитика
4. Обработка возражений

### Фаза 2 (Важно - следующие 4-6 недель)
5. Автоматические напоминания
6. Интеграция с CRM
7. Мультименеджер
8. Резервное копирование

### Фаза 3 (Желательно - следующие 6-8 недель)
9. A/B тестирование
10. Обработка медиа-файлов
11. API для внешних систем
12. Платежи и счета

### Фаза 4 (Nice to have - долгосрочно)
13. Календарь и записи
14. Улучшенное логирование
15. Многоязычная админ-панель
16. Статистика по услугам
17. Временные зоны
18. Расписание работы
19. Автоответы
20. Интеграция с мессенджерами

---

## 🔧 Технические улучшения

### Производительность
- Кэширование часто используемых данных (Redis)
- Оптимизация SQL запросов (индексы)
- Ленивая загрузка данных
- CDN для статических файлов

### Безопасность
- Rate limiting для API
- Двухфакторная аутентификация для админки
- Аудит действий администраторов
- Шифрование чувствительных данных

### Масштабируемость
- Очереди для фоновых задач (Redis Queue)
- Горизонтальное масштабирование
- Балансировка нагрузки
- Микросервисная архитектура (опционально)

---

**Последнее обновление:** 2026-01-19
**Статус:** Предложения для обсуждения
