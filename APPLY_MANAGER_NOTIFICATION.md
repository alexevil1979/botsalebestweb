# 🚀 Применение уведомлений менеджеру при создании лида

## Описание
При создании нового лида бот автоматически отправляет уведомление менеджеру @Branch (или указанному в настройках) с полной информацией о лиде.

## Chat ID менеджера
**MANAGER_CHAT_ID: 640075202**

## Команды для применения на VPS

```bash
# 1. Переход в директорию проекта
cd /ssd/www/bots/botsalebestwebstudio

# 2. Получение изменений из GitHub
git pull origin main

# 3. Добавление chat_id менеджера в .env
# Проверяем, есть ли уже MANAGER_CHAT_ID
grep -q "MANAGER_CHAT_ID" .env && echo "MANAGER_CHAT_ID уже существует" || echo "MANAGER_CHAT_ID=640075202" >> .env

# Если нужно обновить существующий
sed -i 's/^MANAGER_CHAT_ID=.*/MANAGER_CHAT_ID=640075202/' .env

# Или вручную отредактируйте .env и добавьте/обновите строку:
# MANAGER_CHAT_ID=640075202

# 4. Проверка синтаксиса PHP
php -l bot/WebhookHandler.php
php -l core/TelegramAPI.php

# 5. Перезапуск Apache
sudo systemctl restart apache2

# 6. Проверка работы
# Создайте тестовый лид через бота и проверьте, что менеджер получил уведомление
```

## Формат уведомления

Менеджер получит сообщение в формате:

```
🆕 Новый лид #123

👤 Клиент:
• Имя: Иван
• Username: @ivan_user
• Telegram ID: 123456789

📱 Телефон: +79001234567
✉️ Email: ivan@example.com

🎯 Услуга: Создание лендинга

💰 Бюджет: от 50 000 ₽ до 100 000 ₽

📝 Описание задачи:
Нужен лендинг для продажи курсов...

🔗 Просмотр в админке:
https://botsale.1tlt.ru/admin/lead.php?id=123
```

## Настройки в .env

Добавьте или обновите следующие переменные:

```env
# Manager Notification
MANAGER_CHAT_ID=640075202
MANAGER_USERNAME=Branch
ADMIN_URL=https://botsale.1tlt.ru/admin
```

## Приоритет настроек

1. **MANAGER_CHAT_ID** (приоритет) - числовой ID чата менеджера
2. **MANAGER_USERNAME** (fallback) - username менеджера (например, `Branch`)

Если указан `MANAGER_CHAT_ID`, используется он. Если не указан, используется `@MANAGER_USERNAME`.

## Проверка работы

1. Откройте бота в Telegram
2. Пройдите весь процесс создания лида (от приветствия до отправки контакта)
3. После создания лида менеджер должен получить уведомление в Telegram
4. Проверьте логи, если уведомление не пришло:
   ```bash
   tail -f /var/log/apache2/botsalebestwebstudio_error.log
   ```

## Возможные проблемы

### Уведомление не приходит

1. **Проверьте chat_id**: Убедитесь, что `MANAGER_CHAT_ID=640075202` правильно указан в `.env`
2. **Проверьте логи**: 
   ```bash
   tail -20 /var/log/apache2/botsalebestwebstudio_error.log | grep -i "manager\|notification"
   ```
3. **Проверьте, что менеджер начал диалог с ботом**: Бот может отправлять сообщения только пользователям, которые начали с ним диалог
4. **Проверьте права бота**: Убедитесь, что бот имеет права на отправку сообщений

### Ошибка "chat not found"

Это означает, что:
- Менеджер не начал диалог с ботом
- Chat_id указан неверно
- Бот был заблокирован менеджером

**Решение**: Попросите менеджера написать боту `/start` или любое сообщение.

## Дополнительная информация

- Уведомления отправляются сразу после создания лида
- Если отправка уведомления не удалась, ошибка логируется, но не прерывает процесс создания лида
- Ссылка на админку ведет на страницу просмотра конкретного лида

**✅ После применения изменений менеджер будет получать уведомления о каждом новом лиде!**
