# 🔧 Оптимизация структуры услуг - удаление дубликатов

## Описание

Миграция `migrations/optimize_services_structure.php` оптимизирует структуру таблицы `services`:
- Удаляет дубликаты категорий и услуг
- Объединяет старые категории с новыми
- Переносит услуги к правильным категориям
- Исправляет ссылки в таблице `leads`
- Оптимизирует порядок сортировки

## Что делает миграция

### Шаг 1: Перенос услуг от старых категорий к новым
- Категория "Интернет-магазин" (ID 3) → "Веб-разработка" (ID 75)
- Категория "Веб-приложение" (ID 4) → "Веб-разработка" (ID 75)
- Категория "Лендинг" (ID 17) → "Веб-разработка" (ID 75)
- Категория "Корпоративный сайт" (ID 18) → "Веб-разработка" (ID 75)
- Старая "Веб-разработка" (ID 21) → Новая "Веб-разработка" (ID 75)
- Старая "Безопасность и SSL" (ID 22) → Новая "Безопасность и SSL" (ID 79)
- Старая "Дизайн и UI/UX" (ID 23) → Новая "Дизайн и UI/UX" (ID 77)
- Старая "SEO и маркетинг" (ID 24) → Новая "SEO и маркетинг" (ID 78)
- "Автоматизация и интеграции" (ID 25) → "Чат-боты и автоматизация" (ID 76)
- Старая "Прочие услуги" (ID 26) → Новая "Прочие услуги" (ID 82)

### Шаг 2: Объединение дубликатов услуг
- Находит услуги с одинаковыми названиями в одной категории
- Оставляет первую услугу, удаляет остальные
- Обновляет ссылки в таблице `leads`

### Шаг 3: Удаление старых категорий
- Удаляет категории, которые больше не используются
- Проверяет, что у них нет услуг перед удалением

### Шаг 4: Объединение дубликатов категорий
- Находит категории с одинаковыми названиями
- Объединяет их, перенося услуги к основной категории

### Шаг 5: Очистка невалидных категорий
- Находит записи, которые помечены как категории, но имеют цены
- Переносит их в категорию "Веб-разработка"

### Шаг 6: Оптимизация порядка сортировки
- Устанавливает правильный `sort_order` для всех категорий

### Шаг 7: Проверка целостности данных
- Находит услуги с несуществующими `parent_id`
- Исправляет их, перенося в правильные категории

## Команды для применения на VPS

```bash
# 1. Переход в директорию проекта
cd /ssd/www/bots/botsalebestwebstudio

# 2. Получение изменений из GitHub
git pull origin main

# 3. Создание резервной копии БД (РЕКОМЕНДУЕТСЯ!)
mysqldump -u root -p telegram_bot services > services_backup_$(date +%Y%m%d_%H%M%S).sql

# 4. Запуск миграции оптимизации
php migrations/optimize_services_structure.php

# 5. Проверка результата
# Откройте https://botsale.1tlt.ru/admin/services.php
# Проверьте, что:
# - Нет дубликатов категорий
# - Нет дубликатов услуг
# - Все услуги находятся в правильных категориях
# - Количество категорий соответствует ожидаемому
```

## Ожидаемый результат

После выполнения миграции:

### Категории (9 штук):
1. **Веб-разработка** (ID 75) - все услуги веб-разработки
2. **Чат-боты и автоматизация** (ID 76) - боты и автоматизация
3. **Дизайн и UI/UX** (ID 77) - дизайн услуги
4. **SEO и маркетинг** (ID 78) - SEO и маркетинг
5. **Безопасность и SSL** (ID 79) - безопасность
6. **Интеграции и API** (ID 80) - интеграции
7. **Поддержка и обслуживание** (ID 81) - поддержка
8. **Прочие услуги** (ID 82) - прочие
9. **Узкие специалисты** (ID 55) - специалисты с почасовой оплатой

### Услуги:
- Все услуги будут в правильных категориях
- Не будет дубликатов
- Все ссылки в таблице `leads` будут корректными

## Важные замечания

⚠️ **ВНИМАНИЕ**: Миграция изменяет структуру данных. Обязательно создайте резервную копию перед выполнением!

✅ Миграция безопасна:
- Обновляет ссылки в таблице `leads` перед удалением услуг
- Проверяет целостность данных
- Не удаляет данные без проверки

## Восстановление из резервной копии

Если что-то пошло не так:

```bash
# Восстановление из резервной копии
mysql -u root -p telegram_bot < services_backup_YYYYMMDD_HHMMSS.sql
```

---

**Последнее обновление:** 2026-01-19
