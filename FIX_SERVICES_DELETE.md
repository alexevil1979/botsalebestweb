# 🔧 Исправление удаления услуг и добавление группового удаления

## ❌ Проблема

1. Удаление услуг не работало (использовался GET вместо POST)
2. Не было возможности выбрать несколько услуг для удаления
3. Не было функционала группового удаления

## ✅ Исправление

1. **Исправлено удаление одной услуги:**
   - Теперь используется POST-запрос вместо GET
   - Добавлена форма с CSRF-защитой
   - Подтверждение перед удалением

2. **Добавлены чекбоксы для выбора услуг:**
   - Чекбокс в первой колонке таблицы
   - Чекбокс "Выбрать все" в заголовке таблицы
   - Автоматическое обновление состояния "Выбрать все"

3. **Добавлено групповое удаление:**
   - Кнопка "Удалить выбранные" появляется при выборе услуг
   - Подтверждение перед групповым удалением
   - Удаление всех выбранных услуг за один запрос

## 🚀 Команды для применения на VPS

```bash
# 1. Переход в директорию проекта
cd /ssd/www/bots/botsalebestwebstudio

# 2. Получение изменений из GitHub
git pull origin main

# 3. Проверка синтаксиса PHP файлов
php -l admin/services.php

# 4. Перезапуск Apache
sudo systemctl restart apache2

# 5. Проверка работы
# Откройте https://botsale.1tlt.ru/admin/services.php
```

## ✅ Проверка работы

1. **Удаление одной услуги:**
   - Откройте страницу услуг
   - Нажмите "Удалить" у любой услуги
   - Подтвердите удаление
   - Услуга должна быть удалена

2. **Групповое удаление:**
   - Откройте страницу услуг
   - Отметьте несколько услуг чекбоксами
   - Должна появиться кнопка "Удалить выбранные"
   - Нажмите кнопку и подтвердите
   - Все выбранные услуги должны быть удалены

3. **Выбрать все:**
   - Отметьте чекбокс "Выбрать все" в заголовке
   - Все услуги должны быть выбраны
   - Снимите отметку - все должны быть сняты

## 📋 Что изменилось

### admin/services.php

1. **Удаление через POST:**
   ```php
   // Было: GET запрос с параметрами в URL
   // Стало: POST форма с CSRF токеном
   <form method="POST" onsubmit="return confirm('Удалить услугу?');">
       <input type="hidden" name="csrf_token" value="...">
       <input type="hidden" name="action" value="delete">
       <input type="hidden" name="id" value="<?php echo $service['id']; ?>">
       <button type="submit" class="btn-small btn-danger">Удалить</button>
   </form>
   ```

2. **Чекбоксы в таблице:**
   ```html
   <th style="width: 40px;">
       <input type="checkbox" id="selectAll" title="Выбрать все">
   </th>
   ...
   <td>
       <input type="checkbox" name="service_ids[]" value="<?php echo $service['id']; ?>" class="service-checkbox">
   </td>
   ```

3. **Обработка группового удаления:**
   ```php
   elseif ($action === 'delete_multiple') {
       $ids = $_POST['service_ids'] ?? [];
       if (!empty($ids) && is_array($ids)) {
           foreach ($ids as $id) {
               $id = (int)$id;
               if ($id > 0) {
                   Service::delete($id);
               }
           }
       }
   }
   ```

4. **JavaScript для работы с чекбоксами:**
   - Автоматическое обновление кнопки "Удалить выбранные"
   - Синхронизация "Выбрать все" с отдельными чекбоксами
   - Создание формы для группового удаления

## 🎨 Интерфейс

- **Чекбокс "Выбрать все"** - в заголовке первой колонки
- **Чекбоксы услуг** - в первой колонке каждой строки
- **Кнопка "Удалить выбранные"** - появляется справа от "Добавить услугу" при выборе услуг
- **Кнопка "Удалить"** - в колонке "Действия" каждой строки

---

**✅ После применения изменений удаление услуг будет работать корректно!**
