# 📤 Инструкция по публикации в GitHub

## Текущий статус

Git репозиторий инициализирован, файлы добавлены и закоммичены.

## Команды для публикации

Выполните следующие команды в терминале (в директории проекта):

```bash
# 1. Проверьте текущий статус
git status

# 2. Проверьте remote
git remote -v

# 3. Если remote не настроен, добавьте:
git remote add origin https://github.com/alexevil1979/botsalebestweb.git

# 4. Установите ветку main
git branch -M main

# 5. Отправьте код в GitHub
git push -u origin main
```

## Если требуется авторизация

Если GitHub запросит авторизацию, используйте один из вариантов:

### Вариант 1: Personal Access Token
1. Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. При push используйте токен вместо пароля

### Вариант 2: SSH ключ
```bash
# Измените remote на SSH
git remote set-url origin git@github.com:alexevil1979/botsalebestweb.git
git push -u origin main
```

### Вариант 3: GitHub CLI
```bash
gh auth login
git push -u origin main
```

## Альтернатива: через GitHub Desktop или VS Code

1. Откройте проект в VS Code
2. Используйте встроенный Git интерфейс
3. Нажмите "Publish to GitHub"

## Проверка после публикации

После успешного push проверьте:
- https://github.com/alexevil1979/botsalebestweb
- Все файлы должны быть видны в репозитории
