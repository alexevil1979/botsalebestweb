# 🚀 Инструкция по применению изменений на VPS

## 📍 Путь проекта
```
/ssd/www/bots/botsalebestwebstudio
```

---

## ⚡ Быстрое применение изменений

### Шаг 1: Подключение к серверу
```bash
ssh root@your-server-ip
# или
ssh your-username@your-server-ip
```

### Шаг 2: Переход в директорию проекта
```bash
cd /ssd/www/bots/botsalebestwebstudio
```

### Шаг 3: Получение изменений из GitHub
```bash
git pull origin main
```

### Шаг 4: Обновление зависимостей
```bash
composer install --no-dev --optimize-autoloader
```

### Шаг 5: Запуск миграций БД (если были изменения)

**⚠️ ВАЖНО:** Сначала выполните основную миграцию, затем дополнительные:

```bash
# Сначала основная миграция (создает все таблицы)
php migrations/migrate.php

# Затем дополнительные миграции (добавляют колонки в существующие таблицы)
php migrations/add_preferred_language.php
```

**Если таблицы не существуют:**
- Сначала выполните `php migrations/migrate.php` - это создаст все таблицы
- Затем выполните `php migrations/add_preferred_language.php` - это добавит колонку в существующую таблицу

### Шаг 6: Обновление .env файла (если нужно)

Если были изменения в конфигурации, обновите `.env`:

```bash
nano .env
```

**Проверьте следующие параметры:**
```env
# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=telegram_bot
DB_USER=root
DB_PASS=qweasd333123
DB_CHARSET=utf8mb4

# Telegram Bot
TELEGRAM_BOT_TOKEN=8496559310:AAFDB-mRyv4pOh_4Sj2LdtNWYZ4XK0v_DE8
TELEGRAM_WEBHOOK_SECRET=fkew323f32f23f2332f
TELEGRAM_WEBHOOK_URL=https://botsale.1tlt.ru/bot/webhook.php
```

### Шаг 7: Перезагрузка Apache
```bash
sudo systemctl reload apache2
```

---

## 🔄 Использование скрипта деплоя (рекомендуется)

Вместо ручных шагов можно использовать автоматический скрипт:

```bash
cd /ssd/www/bots/botsalebestwebstudio
bash deploy/deploy.sh
```

Скрипт автоматически выполнит:
- ✅ `git pull`
- ✅ `composer install`
- ✅ Миграции БД
- ✅ Очистку Redis
- ✅ Перезагрузку Apache
- ✅ Проверку прав доступа

---

## ✅ Проверка после применения

### 1. Проверка подключения к БД
```bash
php -r "
require 'vendor/autoload.php';
Core\Config::load('.env');
try {
    \$result = Core\Database::fetch('SELECT 1 as test');
    echo '✅ Подключение к БД успешно!';
} catch (Exception \$e) {
    echo '❌ Ошибка: ' . \$e->getMessage();
}
"
```

### 2. Проверка webhook
```bash
php bot/setup-webhook.php
```

### 3. Проверка логов
```bash
# Логи Apache
sudo tail -f /var/log/apache2/botsalebestwebstudio_error.log

# Логи приложения
tail -f logs/php_errors.log 2>/dev/null || echo "Логи не настроены"
```

### 4. Проверка админки
Откройте в браузере: `https://botsale.1tlt.ru/admin`

---

## 🐛 Решение проблем

### Проблема: "git pull" не работает

**Решение:**
```bash
# Проверьте статус
git status

# Если есть конфликты, откатите изменения
git reset --hard origin/main

# Или сохраните изменения
git stash
git pull
git stash pop
```

### Проблема: "composer install" не работает

**Решение:**
```bash
# Обновите composer
composer self-update

# Очистите кеш
composer clear-cache

# Попробуйте снова
composer install --no-dev --optimize-autoloader
```

### Проблема: Ошибки подключения к БД

**Решение:**
1. Проверьте `.env` файл:
   ```bash
   cat .env | grep DB_
   ```

2. Убедитесь, что MySQL запущен:
   ```bash
   sudo systemctl status mysql
   ```

3. Проверьте подключение:
   ```bash
   mysql -h 127.0.0.1 -P 3306 -u root -p
   ```

### Проблема: Apache не перезагружается

**Решение:**
```bash
# Проверьте конфигурацию
sudo apache2ctl configtest

# Если ошибок нет, перезапустите
sudo systemctl restart apache2

# Проверьте статус
sudo systemctl status apache2
```

---

## 📝 Чеклист применения изменений

- [ ] Подключился к серверу
- [ ] Перешел в директорию проекта
- [ ] Выполнил `git pull origin main`
- [ ] Выполнил `composer install --no-dev --optimize-autoloader`
- [ ] Запустил миграции (если нужно)
- [ ] Обновил `.env` (если нужно)
- [ ] Перезагрузил Apache
- [ ] Проверил работу бота
- [ ] Проверил админку

---

## 🔄 Автоматическое применение (GitHub Actions)

Если настроен GitHub Actions, изменения применяются автоматически при push в `main`.

Проверьте статус деплоя:
1. Откройте репозиторий: https://github.com/alexevil1979/botsalebestweb
2. Перейдите в **Actions**
3. Проверьте последний workflow run

---

## 📞 Быстрая справка

**Домен:** `botsale.1tlt.ru`  
**Репозиторий:** `https://github.com/alexevil1979/botsalebestweb.git`  
**Путь на VPS:** `/ssd/www/bots/botsalebestwebstudio`

**Полезные команды:**
```bash
# Статус git
git status

# Последние коммиты
git log -5 --oneline

# Проверка изменений
git diff

# Откат изменений
git reset --hard origin/main
```

---

**✅ После выполнения всех шагов изменения применены на VPS!**
