# 🚀 Гайд по настройке GitHub Actions для автоматического деплоя

## ✅ ВАРИАНТ 1. GitHub Actions (РЕКОМЕНДУЮ)

**Ты находишься на сайте GitHub → нажал "Commit / Merge" → VPS обновился сам.**

### Как это работает

```
GitHub (push / merge)
   ↓
GitHub Actions
   ↓ (SSH)
VPS
   ↓
git pull + deploy.sh
```

### Что нужно

- ✅ VPS с SSH
- ✅ GitHub репозиторий
- ✅ SSH-ключ
- ✅ `.github/workflows/deploy.yml` (уже есть в проекте)

### Плюсы

- ✅ безопасно
- ✅ логируется
- ✅ можно откатить
- ✅ стандарт индустрии
- ✅ **Ты вообще не заходишь на VPS руками**

### Минусы

- ⚠️ 1 раз настроить

---

## 📋 Пошаговая настройка

### Шаг 1: Генерация SSH-ключа на VPS

Подключитесь к вашему VPS:

```bash
ssh root@your-server-ip
```

Создайте SSH-ключ специально для GitHub Actions:

```bash
# Создайте директорию для ключей (если нет)
mkdir -p ~/.ssh
cd ~/.ssh

# Генерируйте новый SSH-ключ (без пароля)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f github_actions_deploy -N ""

# Или если ed25519 не поддерживается:
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f github_actions_deploy -N ""
```

**Результат:** Созданы два файла:
- `github_actions_deploy` - приватный ключ (нужен для GitHub Secrets)
- `github_actions_deploy.pub` - публичный ключ (нужен для VPS)

### Шаг 2: Настройка SSH-ключа на VPS

Добавьте публичный ключ в `authorized_keys`:

```bash
# Скопируйте публичный ключ
cat ~/.ssh/github_actions_deploy.pub

# Добавьте его в authorized_keys
cat ~/.ssh/github_actions_deploy.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

**Проверьте подключение:**

```bash
# С локального компьютера попробуйте подключиться
ssh -i ~/.ssh/github_actions_deploy root@your-server-ip
```

### Шаг 3: Получение приватного ключа

На VPS выполните:

```bash
# Покажите приватный ключ
cat ~/.ssh/github_actions_deploy
```

**Скопируйте весь вывод** (включая `-----BEGIN ...` и `-----END ...`)

**⚠️ ВАЖНО:** Этот ключ секретный! Не публикуйте его нигде.

### Шаг 4: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub: https://github.com/alexevil1979/botsalebestweb

2. Перейдите в **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **"New repository secret"**

4. Добавьте следующие секреты:

#### Secret 1: `VPS_HOST`
- **Name:** `VPS_HOST`
- **Value:** IP адрес или домен вашего VPS (например: `123.45.67.89` или `yourdomain.com`)

#### Secret 2: `VPS_USER`
- **Name:** `VPS_USER`
- **Value:** Пользователь SSH (обычно `root` или ваш пользователь)

#### Secret 3: `VPS_SSH_KEY`
- **Name:** `VPS_SSH_KEY`
- **Value:** Весь приватный SSH-ключ (который вы скопировали на шаге 3)
  ```
  -----BEGIN OPENSSH PRIVATE KEY-----
  b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAACFwAAAAdzc2gtcn
  ...
  -----END OPENSSH PRIVATE KEY-----
  ```

#### Secret 4: `VPS_PATH`
- **Name:** `VPS_PATH`
- **Value:** `/ssd/www/bots/botsalebestwebstudio`

### Шаг 5: Проверка workflow файла

Убедитесь, что файл `.github/workflows/deploy.yml` существует и содержит:

```yaml
name: Deploy to VPS

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to VPS
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          cd ${{ secrets.VPS_PATH }}
          bash deploy/deploy.sh
```

Если файл уже есть в репозитории — всё готово!

### Шаг 6: Тестирование

1. Сделайте небольшое изменение в любом файле (например, добавьте комментарий)

2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Test GitHub Actions deployment"
   git push origin main
   ```

3. Проверьте выполнение:
   - Откройте репозиторий на GitHub
   - Перейдите в **Actions** (вкладка вверху)
   - Вы увидите запущенный workflow "Deploy to VPS"
   - Нажмите на него, чтобы увидеть логи

4. Проверьте результат на VPS:
   ```bash
   ssh root@your-server-ip
   cd /ssd/www/bots/botsalebestwebstudio
   git log -1  # Должен показать ваш последний коммит
   ```

---

## 🔍 Проверка и отладка

### Проблема: Workflow не запускается

**Решение:**
1. Проверьте, что файл `.github/workflows/deploy.yml` находится в репозитории
2. Убедитесь, что вы пушите в ветку `main` или `master`
3. Проверьте вкладку **Actions** на GitHub

### Проблема: SSH подключение не работает

**Решение:**
1. Проверьте, что все Secrets добавлены правильно
2. Убедитесь, что приватный ключ скопирован полностью (включая BEGIN/END строки)
3. Проверьте, что публичный ключ добавлен в `authorized_keys` на VPS
4. Проверьте права доступа:
   ```bash
   chmod 600 ~/.ssh/authorized_keys
   chmod 700 ~/.ssh
   ```

### Проблема: "Permission denied"

**Решение:**
1. Проверьте, что пользователь SSH имеет права на директорию проекта:
   ```bash
   sudo chown -R $USER:$USER /ssd/www/bots/botsalebestwebstudio
   ```

2. Или используйте `root` пользователя в Secrets

### Проблема: "deploy.sh: command not found"

**Решение:**
1. Убедитесь, что файл `deploy/deploy.sh` существует
2. Проверьте права на выполнение:
   ```bash
   chmod +x /ssd/www/bots/botsalebestwebstudio/deploy/deploy.sh
   ```

### Просмотр логов

В GitHub:
1. Откройте репозиторий → **Actions**
2. Выберите нужный workflow run
3. Нажмите на job "deploy"
4. Разверните шаги, чтобы увидеть детальные логи

---

## 🔐 Безопасность

### Рекомендации:

1. ✅ Используйте отдельный SSH-ключ только для GitHub Actions
2. ✅ Не используйте ваш основной SSH-ключ
3. ✅ Регулярно ротируйте ключи (раз в 3-6 месяцев)
4. ✅ Ограничьте права пользователя SSH (если возможно)
5. ✅ Используйте не-root пользователя (если возможно)

### Создание отдельного пользователя для деплоя (опционально):

```bash
# Создайте пользователя
sudo adduser deployer

# Добавьте его в группу www-data
sudo usermod -aG www-data deployer

# Дайте права на директорию проекта
sudo chown -R deployer:www-data /ssd/www/bots/botsalebestwebstudio

# Настройте SSH-ключ для этого пользователя
sudo su - deployer
mkdir -p ~/.ssh
cat github_actions_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

Затем в GitHub Secrets используйте:
- `VPS_USER` = `deployer`

---

## 📊 Мониторинг деплоев

### Уведомления

GitHub Actions может отправлять уведомления:
1. В **Settings** → **Notifications** → **Actions**
2. Настройте уведомления о статусе workflow

### Статусы деплоя

- ✅ **Зеленый** - деплой успешен
- ❌ **Красный** - деплой провалился (смотрите логи)
- 🟡 **Желтый** - деплой в процессе

---

## 🔄 Откат изменений

Если что-то пошло не так:

### Вариант 1: Через Git на VPS
```bash
ssh root@your-server-ip
cd /ssd/www/bots/botsalebestwebstudio
git log  # Найдите нужный коммит
git reset --hard <commit-hash>  # Откатитесь к нему
sudo systemctl reload apache2
```

### Вариант 2: Через GitHub
1. Откройте репозиторий → **Commits**
2. Найдите нужный коммит
3. Создайте новый коммит, откатив изменения
4. Push → GitHub Actions автоматически задеплоит

---

## ✅ Чеклист настройки

- [ ] SSH-ключ создан на VPS
- [ ] Публичный ключ добавлен в `authorized_keys`
- [ ] Приватный ключ скопирован
- [ ] GitHub Secrets добавлены:
  - [ ] `VPS_HOST`
  - [ ] `VPS_USER`
  - [ ] `VPS_SSH_KEY`
  - [ ] `VPS_PATH`
- [ ] Файл `.github/workflows/deploy.yml` существует
- [ ] Тестовый деплой выполнен успешно
- [ ] Проверены логи в GitHub Actions

---

## 🎯 После настройки

**Теперь вы можете:**

1. Делать изменения локально
2. Коммитить и пушить в GitHub
3. **Автоматически** изменения применятся на VPS

**Вы больше не заходите на VPS вручную для обновления!**

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи в GitHub Actions
2. Проверьте SSH подключение вручную
3. Убедитесь, что все Secrets добавлены правильно
4. Проверьте права доступа на VPS

---

**🎉 Готово! Теперь каждый push в `main` автоматически обновит ваш VPS!**

---

## 🔄 Ручное применение изменений

Если нужно применить изменения вручную (без GitHub Actions), используйте:

**📖 [APPLY_CHANGES_VPS.md](APPLY_CHANGES_VPS.md)** - Инструкция по применению изменений на VPS
